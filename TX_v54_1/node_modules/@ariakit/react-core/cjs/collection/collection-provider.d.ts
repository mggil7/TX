import type { ReactElement, ReactNode } from "react";
import type { PickRequired } from "@ariakit/core/utils/types";
import type { CollectionStoreItem, CollectionStoreProps } from "./collection-store.js";
type Item = CollectionStoreItem;
/**
 * Provides a collection store to CollectionItem components.
 * @see https://ariakit.org/components/collection
 * @example
 * ```jsx
 * <CollectionProvider>
 *   <CollectionItem />
 *   <CollectionItem />
 *   <CollectionItem />
 * </CollectionProvider>
 * ```
 */
export declare function CollectionProvider<T extends Item = Item>(props: PickRequired<CollectionProviderProps<T>, "items" | "defaultItems">): ReactElement;
export declare function CollectionProvider(props?: CollectionProviderProps): ReactElement;
export interface CollectionProviderProps<T extends Item = Item> extends CollectionStoreProps<T> {
    children?: ReactNode;
}
export {};
