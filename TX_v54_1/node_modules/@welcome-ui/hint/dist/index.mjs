// src/index.tsx
import React from "react";
import { forwardRef } from "@welcome-ui/system";

// src/styles.ts
import styled, { css, system, th } from "@xstyled/styled-components";
var VARIANTS = {
  error: "colors.danger-500",
  info: "colors.info-500",
  success: "colors.success-500",
  warning: "colors.warning-500"
};
var Hint = styled.div(
  ({ variant }) => css`
    ${th("hints")};
    color: ${th(VARIANTS[variant]) || void 0};
    margin-top: xs;
    display: flex;
    align-items: center;
    ${system};
  `
);

// src/index.tsx
var Hint2 = forwardRef(
  ({ children, dataTestId, variant, ...rest }, ref) => {
    return /* @__PURE__ */ React.createElement(Hint, { "data-testid": dataTestId, ref, variant, ...rest }, children);
  }
);
Hint2.displayName = "Hint";
var StyledHint = Hint;
export {
  Hint2 as Hint,
  StyledHint
};
