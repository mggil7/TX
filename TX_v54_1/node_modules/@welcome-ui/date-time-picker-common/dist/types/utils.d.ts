import { Locale } from 'date-fns';
import { Option, SelectOptions } from '@welcome-ui/select';
export declare const DEFAULT_DATE: Date;
export declare const getDate: (date: string | number | Date, interval?: number) => Date;
export declare const getMonths: (locale: Locale) => SelectOptions['options'];
export declare const getYears: (startYear: number, endYear: number) => Option[];
