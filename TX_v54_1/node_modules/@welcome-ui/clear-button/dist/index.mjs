// src/index.tsx
import React from "react";
import { CrossIcon } from "@welcome-ui/icons";
import { forwardRef } from "@welcome-ui/system";

// src/styles.ts
import styled, { css, keyframes, th } from "@xstyled/styled-components";
import { Button } from "@welcome-ui/button";
var fade = keyframes`
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
`;
var fadeRule = css`
  ${fade}
`;
var ClearButton = styled(Button)`
  pointer-events: auto;
  animation: ${th("transitions.medium")};
  animation-name: ${fadeRule};
`;

// src/index.tsx
var ClearButton2 = forwardRef(
  ({ size = "xs", ...rest }, ref) => /* @__PURE__ */ React.createElement(ClearButton, { ref, shape: "circle", size, title: "Clear", variant: "ghost", ...rest }, /* @__PURE__ */ React.createElement(CrossIcon, { size: "xxs" }))
);
var StyledClearButton = ClearButton;
export {
  ClearButton2 as ClearButton,
  StyledClearButton
};
